---
import PublicLayout from '../../layouts/PublicLayout.astro';
import JobDetails from '../../components/jobs/JobDetails';

const { slug } = Astro.params;
---

<PublicLayout>
  <JobDetails slug={slug || ''} client:load />
</PublicLayout>
